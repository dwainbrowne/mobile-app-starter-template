import { View } from 'react-native';

/**
 * Placeholder screen for the center "+" tab button.
 * This screen never actually renders - the tab button opens
 * the QuickActionsMenu modal instead.
 */
export default function AddScreen() {
  return <View />;
}
